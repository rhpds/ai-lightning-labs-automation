{{ if .Values.rhaiCodeDemoPlugin.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rhai-code-demo-plugin
  namespace: openshift-gitops
spec:
  project: platform
  source:
    repoURL: {{ .Values.rhaiCodeDemoPlugin.git.repoURL }}
    targetRevision: {{ .Values.rhaiCodeDemoPlugin.git.targetRevision }}
    path: {{ .Values.rhaiCodeDemoPlugin.git.path }}
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
