{{ if .Values.imagePuller.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: image-puller
  namespace: openshift-gitops
spec:
  project: platform
  source:
    repoURL: {{ .Values.imagePuller.git.repoURL }}
    targetRevision: {{ .Values.imagePuller.git.targetRevision }}
    path: {{ .Values.imagePuller.git.path }}
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
