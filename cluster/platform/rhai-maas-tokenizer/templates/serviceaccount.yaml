---
{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "maas-tokenizer.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "maas-tokenizer.labels" . | nindent 4 }}
{{- end }}
