---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "mcp-weather.fullname" . }}
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: {{ include "mcp-weather.fullname" . }}
    weight: 100
  wildcardPolicy: None
status: {}
